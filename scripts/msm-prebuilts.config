# The common kernel is built by scripts/build-common.sh.  Reuse that dist tree
# instead of asking the Qualcomm build config to launch a nested common build.
export GKI_BUILD_CONFIG=""
export GKI_PREBUILTS_DIR="${GKI_DIST_DIR}"
