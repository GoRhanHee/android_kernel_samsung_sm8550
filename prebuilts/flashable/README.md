# Deprecated custom recovery package

The active build and CI distribution no longer generate or recommend this
legacy installer. Kernel images are now distributed as an AnyKernel3 package
using the `prebuilts/AnyKernel3` submodule and `prebuilts/anykernel.sh`.

The template remains in this directory only for historical reference and is
not called by `build.sh`.
